╔══════════════════════════════════════════════════════════════════════════════╗
║  🔧 SOLUCIÓN: MainActivity.kt - CÓMO CREAR EL ARCHIVO CORRECTAMENTE         ║
╚══════════════════════════════════════════════════════════════════════════════╝

⚠️ EL CÓDIGO NO CAMBIA LA EXTENSIÓN - ES CÓMO LO CREAS EN ANDROID STUDIO

═══════════════════════════════════════════════════════════════════════════════

📝 PASOS EXACTOS PARA CREAR MainActivity.kt:

PASO 1: ELIMINA EL MainActivity QUE YA EXISTE
----------------------------------------------
1. En Android Studio, busca el MainActivity actual
2. Click derecho en el archivo > Delete
3. Confirma la eliminación

PASO 2: CREA UN NUEVO MainActivity CORRECTAMENTE
-------------------------------------------------
1. Ve a la carpeta: com.example.proyecto1 (la RAÍZ, no dentro de ninguna subcarpeta)

2. Click derecho en "proyecto1" > New > Kotlin Class/File

3. ⚠️ IMPORTANTE - En la ventana que aparece:
   
   ┌─────────────────────────────────────┐
   │ Name: MainActivity                  │  ← Escribe SIN .kt
   │                                     │
   │ [▼] File        ← SELECCIONA ESTO  │  ⚠️ MUY IMPORTANTE
   │     Class                           │
   │     Interface                       │
   │     Object                          │
   │     Enum class                      │
   └─────────────────────────────────────┘
   
   DEBE DECIR "File" - NO selecciones "Class"

4. Presiona Enter

PASO 3: PEGA EL CÓDIGO COMPLETO
--------------------------------
Android Studio creará un archivo vacío o con package solamente.

BORRA TODO lo que haya en el archivo y pega ESTE CÓDIGO COMPLETO:

═══════════════════════════════════════════════════════════════════════════════

package com.example.proyecto1

import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.activity.compose.setContent
import androidx.activity.enableEdgeToEdge
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.foundation.layout.padding
import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.Scaffold
import androidx.compose.material3.Surface
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.lifecycle.viewmodel.compose.viewModel
import androidx.navigation.compose.rememberNavController
import com.example.proyecto1.navigation.ReservaApp
import com.example.proyecto1.ui.theme.ReservaSalasTheme
import com.example.proyecto1.viewmodel.ReservaViewModel

class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()
        setContent {
            ReservaSalasTheme {
                AppContent()
            }
        }
    }
}

@Composable
private fun AppContent() {
    val navController = rememberNavController()
    val viewModel: ReservaViewModel = viewModel()
    
    Scaffold(
        modifier = Modifier.fillMaxSize()
    ) { innerPadding ->
        Surface(
            modifier = Modifier
                .fillMaxSize()
                .padding(innerPadding),
            color = MaterialTheme.colorScheme.background
        ) {
            ReservaApp(
                navController = navController,
                viewModel = viewModel
            )
        }
    }
}

═══════════════════════════════════════════════════════════════════════════════

PASO 4: GUARDA EL ARCHIVO
--------------------------
Ctrl+S o File > Save All

═══════════════════════════════════════════════════════════════════════════════

✅ VERIFICA QUE ESTÉ CORRECTO:

1. El archivo DEBE estar en la ruta:
   app/src/main/java/com/example/proyecto1/MainActivity.kt

2. En la vista de proyecto, DEBE aparecer FUERA de las carpetas:
   - NO en data/
   - NO en model/
   - NO en ui/
   - SINO en la RAÍZ de com.example.proyecto1/

3. Si en la vista de Android Studio dice "MainActivity" (sin .kt), ESTÁ BIEN
   Android Studio oculta las extensiones en la vista de proyecto
   El archivo SÍ es MainActivity.kt

═══════════════════════════════════════════════════════════════════════════════

❌ SI EL PROBLEMA PERSISTE:

OPCIÓN A: Crear desde la terminal
-----------------------------------
1. Abre Terminal en Android Studio (abajo)
2. Navega a: cd app/src/main/java/com/example/proyecto1/
3. Ejecuta: touch MainActivity.kt
4. Ahora en Android Studio verás el archivo
5. Abre el archivo y pega el código completo

OPCIÓN B: Crear como archivo de texto
--------------------------------------
1. Click derecho en proyecto1 > New > File
2. Nombre: MainActivity.kt (CON .kt al final)
3. Presiona Enter
4. Abre el archivo y pega el código completo

═══════════════════════════════════════════════════════════════════════════════

🔍 DIAGNÓSTICO DEL PROBLEMA:

Si estás viendo que el archivo "cambia" o "deja de ser .kt", es porque:

❌ Seleccionaste "Class" en lugar de "File"
   → Android Studio crea una clase y agrega código extra
   
❌ Estás pegando el código en el lugar equivocado
   → Borra TODO el contenido del archivo y pega desde la línea 1

❌ Android Studio está mostrando la vista "Android" en lugar de "Project"
   → Cambia la vista arriba a la izquierda de "Android" a "Project"

❌ El archivo está dentro de otra carpeta
   → MainActivity.kt DEBE estar en la RAÍZ de com.example.proyecto1/

═══════════════════════════════════════════════════════════════════════════════

✅ EL CÓDIGO ES CORRECTO - EL PROBLEMA ES CÓMO LO CREAS

El código que te di es 100% correcto y funcional. El problema NO es el código,
sino el procedimiento para crear el archivo en Android Studio.

Sigue los pasos exactos de arriba y funcionará correctamente.

═══════════════════════════════════════════════════════════════════════════════

📱 DESPUÉS DE CREAR CORRECTAMENTE:

1. File > Sync Project with Gradle Files
2. Build > Clean Project  
3. Build > Rebuild Project
4. Run ▶️

═══════════════════════════════════════════════════════════════════════════════
